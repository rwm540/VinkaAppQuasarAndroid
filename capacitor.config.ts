import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.audiorecorder',
  appName: 'ضبط صدا و تماس',
  webDir: 'dist/spa'
};

export default config;
